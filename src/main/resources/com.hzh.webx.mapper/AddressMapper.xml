<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hzh.webx.mapper.AddressMapper">
    <insert id="addAddress" parameterType="Address">
        INSERT INTO addresses (address_id, country, province, city, street,zip_code)
        VALUES (#{addressId}, #{country}, #{province}, #{city}, #{street},#{zipCode})
    </insert>
</mapper>